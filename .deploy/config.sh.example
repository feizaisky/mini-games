# Deploy Configuration
# 部署配置文件（可选）

# ============== 生产服务器配置 ==============
# 默认值（与 deploy.sh 中的默认值相同）
# DEPLOY_SERVER="web-server"
# DEPLOY_DIR="/usr/share/nginx/html/"

# 自定义配置（如果需要，取消注释并修改）
# DEPLOY_SERVER="user@your-server.com"
# DEPLOY_DIR="/var/www/html/mini-games"

# 说明：
# - 如果不创建此文件，将使用默认配置
# - 只需配置需要修改的变量
# - 服务器地址格式: user@hostname 或 hostname
